apiVersion: v1
kind: Service
metadata:
  name: ai-orchestrator-svc
  namespace: default
  labels:
    app: ai-orchestrator
spec:
  type: ClusterIP
  selector:
    app: ai-orchestrator
  ports:
    - name: http
      port: 80
      targetPort: 80
